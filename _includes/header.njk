<header class="header">
  <a href="/" class="home-link">{{ metadata.title }}</a>

  <!-- Read more about `eleventy-navigation` at https://www.11ty.dev/docs/plugins/navigation/ -->
  <nav>
    <h2 class="visually-hidden">Top level navigation menu</h2>
    <ul class="nav">
      {%- for entry in collections.all | eleventyNavigation %}
      <li class="nav-item">
        <a href="{{ entry.url }}"{% if entry.url == page.url %} aria-current="page"{% endif %}>{{ entry.title }}</a>
      </li>
      {%- endfor %}
    </ul>
    <!-- Theme Switcher Dropdown in Navigation -->
    <label for="theme-select">pick your flavor:</label>
        <select id="theme-select" onchange="setTheme(this.value)">
      <option value="latte">latte</option>
      <option value="rosepine-light">rosé pine</option>
      <option value="mocha">mocha</option>
      <option value="macchiato">macchiato</option>
      <option value="frappe">frappe</option>
      <option value="kementari">kementari</option>
    </select>
    <script src="./layouts/theme-switcher.js"></script>
  </nav>
</header>
